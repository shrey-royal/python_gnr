# here we are executing the commands in the CMD using python

# import os

# os.system('cmd /k "date"')  # to get the date
# os.system('cmd /c "time"')  # to get the time

# /k -> to keep the CMD open after the execution of the command
# /c -> to close the CMD after the execution of the command


# os.system('cmd /k "color a & date & time"')  # to create a directory
# print(os.getenv('path'))  # to get the path of the environment variable

# print(os.get_exec_path())  # to get the path of the executable file

# import os

# print(os.uname())